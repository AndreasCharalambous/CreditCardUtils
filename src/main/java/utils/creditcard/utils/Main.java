package utils.creditcard.utils;

import utils.creditcard.enums.CreditCardType;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Main {

//    public static void main(String[] args) {
//        final List<Validator> tests = new ArrayList<>();
//
//        Arrays.stream(CreditCardType.values()).forEach(type->{
//            tests.add(new Validator.ValidatorBuilder().setTestIinRange((s) -> s.startsWith(1)).setTestIsActive(testIsActive).setTestSize(testSize).setTestAlgorithm(testAlgorithm).createValidator());
//        });
//
//        System.out.println(tests.get(0).getTestIinRange().test("G"));
//    }
}
